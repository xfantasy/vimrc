author : x.fantasy@gmail.com
history : 
	2011.11.08 -- add

setp 1. git clone to ~/.vim
	git clone git@github.com:xfantasy/vimrc.git .vim
step 2. create a symbolic link file '~/.vimr' link to ~/.vim/vimrc
	ln -s ~/.vim/vimrc ~/.vimrc
step 3. create a symbolic link file '~/.gvimrc' link to ~/.vim/gvimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc
